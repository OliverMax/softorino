{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npx lerna run build",

    "dev": "npx lerna run dev",
    "dev:client": "npx lerna run dev --scope=@softorino/client",
    "dev:server": "npx lerna run dev --scope=@softorino/server"
  },
  "dependencies": {},
  "devDependencies": {
    "lerna": "^7.2.0"
  },
  "engines": {
    "node": "^16.20.2"
  }
}
